<style>
    /* Custom styles for the table */
    .table-container {
        margin-top: 50px;
    }

    .table-container h1 {
        text-align: center;
        margin-bottom: 30px;
    }

    .search-input {
        width: 300px;
        margin-bottom: 20px;
    }
</style>
<section>
    <div class="container mt-2">
        {{!-- <div class="row mt-5">
            {{#each products}}
            <div class="col-12 col-sm-6 col-md-6 col-lg-4 col-xl-3 mb-5">
                <div class="card mx-auto" style="width: 16rem;">
                    <img src="/images/product-images/{{this._id}}.png" class="card-img-top" alt="phone-image"
                        style="height: 17rem;">
                    <hr class="text-secondary">
                    <div class="card-body pb-1">
                        <div class="" style="display: flex; justify-content: end;">
                            <div class="" style="margin-right: auto;">
                                <h5 class="card-title">{{this.Name}}</h5>
                            </div>
                            <div class="">
                                <p class="card-text"><small>₹{{this.Price}}</small></p>
                            </div>
                        </div>
                        <p class="card-text">{{this.Description}}</p>
                        <div class="row">
                            <button onclick="addToCart('{{this._id}}')" class="btn btn-primary">Add to Cart</button>
                        </div>
                    </div>
                </div>
            </div>
            {{/each}}
        </div> --}}

        <div class="container">
            <h1>Searchable Table</h1>
            <input type="text" id="searchInput" placeholder="Search...">
            <table class="table table-striped" id="dataTable">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Title</th>
                        <th>Price</th>
                        <th>Availale Stock</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each products}}
                    <tr>
                        <td>1</td>
                        <td>{{this.Name}}</td>
                        <td>₹{{this.Price}}</td>
                        <td>{{this.Description}}</td>
                        <td><button onclick="addToCart('{{this._id}}')" class="btn btn-primary">Add to Cart</button>
                        </td>
                    </tr>
                    {{/each}}

                </tbody>
            </table>
        </div>
    </div>
</section>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js"></script>

<script>
    // Add event listener to search input
    const searchInput = document.getElementById('searchInput');
    searchInput.addEventListener('keyup', function () {
        const value = this.value.toLowerCase();
        const table = document.getElementById('dataTable');
        const rows = table.getElementsByTagName('tr');

        // Iterate over each row and hide/show based on search input
        for (let i = 0; i < rows.length; i++) {
            const cells = rows[i].getElementsByTagName('td');
            let found = false;
            for (let j = 0; j < cells.length; j++) {
                const cellValue = cells[j].innerText.toLowerCase();
                if (cellValue.includes(value)) {
                    found = true;
                    break;
                }
            }
            if (found) {
                rows[i].style.display = '';
            } else {
                rows[i].style.display = 'none';
            }
        }
    });
</script>
<script>
    function addToCart(productId) {
        $.ajax({
            url: "/add-to-cart/" + productId,
            method: "get",
            success: (response) => {
                if (response.status) {
                    let count = $("#cart-count").html();
                    count = parseInt(count) + 1;
                    $("#cart-count").html(count);
                }
            },
        });
    }
</script>